import "./../css/app.scss";

import "./js/app-set-user-name-in-title";
import "./js/app-aside";

/*
import "./js/app-transition";
import "./../db";


import "./js/app-services-and-masters";
import "./../auth/js/auth-footer";

import { showNotification } from "../auth/js/auth-notification";

function resetServicesAndMasters() {
	const button = document.querySelector(".actions__btn[data-action='reset']");
	button.addEventListener("click", () => {
		let storage = checkStorageOnline();
		storage.removeItem("deletedMasters");
		storage.removeItem("deletedServices");
		storage.removeItem("addedServices");
		storage.removeItem("addedMasters");
	});
}

function returnToIndexHtml() {
	const button = document.querySelector(".actions__btn[data-action='return']");
	button.addEventListener("click", () => {
		sessionStorage.removeItem("online");
		localStorage.removeItem("online");
		window.location.assign("/index.html");
	});
}

resetServicesAndMasters();
returnToIndexHtml();
 */
